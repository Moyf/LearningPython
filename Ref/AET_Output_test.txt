| 1	| 关键帧速度	| `Ctrl-Alt-K`/`Command-Shift-K` 调节进入速度和输出速度，可以快速实现生动的*缓动*的功能；用`Command-Alt-K`可以调出*关键帧插值*窗口；	|
| 2	| 迷你流程图	| 在时间轴按`Tab`可以唤出迷你流程图；  
  
  
  |序号	|描述	|操作方法	|
  |---	|---	|---	|
  |1		|关键帧速度	|原始的办法是调整关键帧缓入（F9）然后在速度图表里头调节具体的速度变化；但更快的办法是用**关键帧速度** `Ctrl-Alt-K`/`Command-Shift-K` 调节进入速度和输出速度，这同样可以实现**缓动**的功能；
  |2		|-|-|	|
